# Optional. Without keys the app uses the optimized demo fare engine
# (economically grounded inversions, instant). With keys it also shops
# live GDS offers via Amadeus Self-Service.
AMADEUS_CLIENT_ID=
AMADEUS_CLIENT_SECRET=
AMADEUS_HOSTNAME=test

CACHE_TTL_SECONDS=300
MAX_HIDDEN_CANDIDATES=16
MAX_CONCURRENCY=6
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
